package ClassesAndMethods;

public class Data {
    int day;
    int month;
    int year;
    String formattedData (int initialDay, int initialMonth, int initialYear){

        return initialMonth + "/" + initialDay + "/" + initialYear;
    }
}
